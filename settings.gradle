dependencyResolutionManagement {
    apply from: 'dependencies.gradle'

    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "TVmazeApp"
include ':app'
